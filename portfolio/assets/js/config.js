/* =====================================================================
   Supabase 設定 — ここに自分のプロジェクトの値を貼ってください
   ---------------------------------------------------------------------
   取得場所: Supabase ダッシュボード → Project Settings → API
     - URL      : 「Project URL」
     - anon key : 「Project API keys」の "anon public"（公開して安全な鍵）
   ※ anon キーは公開前提の鍵です。書き込みは Row Level Security(RLS) で保護します。
   ※ ここが空のままでもサイト本体は動きます（ブログはセットアップ案内を表示）。

   セットアップ手順の全体は README.md を参照してください。
   ===================================================================== */
window.NEKONA_CONFIG = {
  SUPABASE_URL: '',       // 例: 'https://xxxxxxxxxxxx.supabase.co'
  SUPABASE_ANON_KEY: '',  // 例: 'eyJhbGciOi...'（anon public）
};
